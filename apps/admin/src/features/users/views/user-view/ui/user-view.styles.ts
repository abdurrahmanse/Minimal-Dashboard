import type { SxProps, Theme } from '@mui/material/styles';

export const headerBoxStyle: SxProps<Theme> = {
  mb: 5,
  display: 'flex',
  alignItems: 'center',
};

export const titleStyle: SxProps<Theme> = { flexGrow: 1 };

export const tableContainerStyle: SxProps<Theme> = { overflow: 'unset' };

export const tableStyle: SxProps<Theme> = { minWidth: 800 };
